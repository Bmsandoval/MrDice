package internal

func playGame() (string, int) {
	// Create 4 players

	// Select a random starting player

	// 4 Rounds:
	// start with selected starting player
	// Roll available dice
	// Select at least one die
	// Repeat rolling for player until all dice have been rolled
	// Repeat for next player
	// At end of round, starting player changes to a new player

	// Lowest score of each round wins, but the final winner is who matters
	// Return winning player and score
	return "", 0
}
